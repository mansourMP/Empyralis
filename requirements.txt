fastapi==0.135.3
sentry-sdk[fastapi]>=2.57.0
SQLAlchemy>=2.0.0
uvicorn[standard]==0.42.0
sse-starlette==3.0.3
openai
python-dotenv>=1.0.0
certifi
prompt_toolkit>=3.0.0
cryptography>=42.0.0
pydantic>=2.0.0
PyYAML>=6.0
openpyxl>=3.1.0
requests>=2.31.0
httpx>=0.27.0
Pillow>=10.0.0
av>=12.0.0
mcp[cli]==1.26.0
starlette==1.0.1
# MAN-310: second, selectable Sage turn engine (server_modules/claude_agent_
# sdk_bridge.py), default off. Verified clean against every pin above with
# zero version bumps and a clean `pip check` — see spikes/man-310-claude-
# agent-sdk/requirements-spike.txt. Also requires Node.js + the Claude Code
# CLI (`claude`) on PATH wherever the flag is actually turned on — the SDK
# spawns it as a subprocess (claude_agent_sdk_bridge.py's docstring).
claude-agent-sdk==0.2.130
croniter>=2.0.0
dropbox>=11.36.0
boto3>=1.34.0
discord.py>=2.4.0
PyNaCl>=1.5.0
SpeechRecognition>=3.10.0
playwright>=1.40.0
pyautogui>=0.9.54
pytesseract>=0.3.10
pyperclip>=1.9.0
psutil>=6.0.0
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp
asyncpg>=0.30.0
pytest-asyncio>=0.24.0
paramiko>=3.5.0
# Removed 2026-08-13: torch, sentence-transformers, transformers,
# opencv-python. Together they installed ~2.7GB of NVIDIA CUDA runtime onto a
# GPU-less VPS. sentence-transformers had exactly one consumer -- the
# embedding branch of agent_memory._semantic_search -- whose model weights
# were NEVER downloaded in production, so that branch was unreachable and the
# keyword fallback was what actually ran; transformers, opencv-python and
# torch itself had zero importers of any kind (torch came in only as
# sentence-transformers' own dependency). Off-doctrine as well as unused:
# CLAUDE.md records rejecting RAG/embeddings for agentic search, and the
# 2026-08-08 RAG deletion took lancedb+pandas while missing these. Pillow and
# av are KEPT -- av has real importers and Pillow is a small, widely
# transitive dependency.
