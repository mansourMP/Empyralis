# Generated by `xcodegen generate` from project.yml — project.yml is the
# source of truth. Committing the generated project guarantees a conflict
# on every regeneration, and lets the two drift apart silently.
Empyralis.xcodeproj/

# Xcode local state
xcuserdata/
*.xcuserstate
DerivedData/
build/

# Verification screenshots taken during development — regenerate with
# `xcrun simctl io <device> screenshot`.
*.png
