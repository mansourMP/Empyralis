# Dependencies
node_modules/
*/node_modules/
venv/
.venv/
env/
python_engine/.venv/
python_engine/venv/

# Build outputs
dist/
.next/
.next-e2e/
out/
build/
*.tsbuildinfo
target/
src-tauri/target/

# Databases and sensitive data
*.db
*.sqlite
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# Vercel-pulled env files carry a short-lived VERCEL_OIDC_TOKEN — never commit.
.env.vercel*

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local generated runtime/certification scratch
.tmp/
reports/agent-computer-launch-cert/
reports/transcript-migrations/

# OS files
.DS_Store
Thumbs.db

# Python caches / bytecode
__pycache__/
*.pyc
*.pyo

# Local scratch copies
*.bak

# Empyralis local runtime state (machine-local)
.orion/
.orion-stack/
.orion_approval_audit.json
.orion_channel_events.json
.orion_channel_events.json.bak.*
.orion_credentials_vault.json
.orion_credentials_vault.json.bak.*
# Phase 3C: the JSON credential vault is retired (secrets live in Postgres).
# The file is kept only as a cold backup and must never be committed.
credentials.json
credentials.json.*
**/state/vault/
.orion_idempotency_log.json
.orion-media/
.orion-object-store/
.orion_run_history.json
.orion_runtime_skills.json
.orion_runtime_state.db
.orion_runtime_state.db-*
.orion_setup_sessions.json
.orion_telegram_autopilot_state.json
.orion_telegram_autopilot_state.json.bak.*
.orion_telegram_chat_onboarding.json
.orion_telegram_chat_profiles.json
.orion_whatsapp_autopilot_state.json
.orion_vault_key
.orion-validation/
.orion-artifacts/
.gws-config/
*.bak.migration
screenshots/
test_payload.json
dead_code_report.txt

# Platform frontend (separate Next.js project — add as submodule when ready)
platform/

# Archive notes
# backend/ is a legacy NestJS runtime era archive target. Keep tracked until final deletion.
# desktop/ is a legacy Electron shell era archive target. Keep tracked until final deletion.

.local-dev-state/
.claude/
# _archive/ — tracked. Historical code preserved for auditability (Phase U2/UA).
config/cache/
config/codex.sqlite3
config/codex.toml
config/secret_key
dump.rdb
*.rdb
*.dmg
graphify-out/

# Packer image build (deploy/packer) — local build artifacts only. The image ID
# lives in the workflow output and in R2, never in the repo.
deploy/packer/packer-manifest.json
.packer.d/
packer_cache/
crash.log
